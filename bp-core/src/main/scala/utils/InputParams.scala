package main.scala.utils

case class InputParamProc(felem: Option[Int], selem: Option[Int], param: String, args: List[String])

trait Assembled {
  // ????????
}

trait AssembledFrame extends Assembled {
  // ????????
}

trait AssembledBProcess extends Assembled {
  // ????????
}